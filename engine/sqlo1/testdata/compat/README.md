# Compat sections

fixtures.txt holds the STRING, BITMAP, HLL, HASH, SET, ZSET, GEO,
LIST, and STREAM manifest rows from spec doc 12 section 1, generated by gen.py against
a real redis-server 8.8.0 and replayed byte for byte by
TestCompatRedisParity through the sqlo1 dispatch path.

Regenerate with:

    redis-server --port 7399 --save '' --appendonly no --daemonize no &
    python3 gen.py 7399 > fixtures.txt

## Where each manifest row is diffed

| Doc 12 row | Coverage |
|------------|----------|
| GET, SET (all flags), SETNX, SETEX, PSETEX, GETDEL, GETEX, STRLEN, SUBSTR | STRING section here |
| APPEND, GETRANGE, SETRANGE | STRING section here |
| INCR, DECR, INCRBY, DECRBY, INCRBYFLOAT | STRING section here (see the float note below) |
| MGET, MSET, MSETNX | STRING section here |
| LCS | canonical rows here; the full option matrix is pinned live in lcs_test.go |
| SETBIT, GETBIT, BITFIELD, BITFIELD_RO | BITMAP section here |
| BITCOUNT, BITPOS | BITMAP section here |
| BITOP | BITMAP section here |
| PFADD, PFCOUNT, PFMERGE, PFDEBUG, PFSELFTEST | surface rows here; deep parity (envelope bytes, estimator, debug views, promotion) in testdata/hll |
| HSET, HSETNX, HMSET, HGET, HMGET, HDEL, HEXISTS, HSTRLEN, HLEN | HASH section here |
| HINCRBY, HINCRBYFLOAT | HASH section here (same float note as INCRBYFLOAT) |
| HGETALL, HKEYS, HVALS, HSCAN | HASH section here (listpack-tier order; segmented-tier order is engine-defined and pinned in hashiter tests) |
| HRANDFIELD | deterministic rows here (misses, count 0, one-field forcing); distribution lives in the hrand lab |
| HGETEX, HGETDEL | HASH section here |
| HEXPIRE, HPEXPIRE, HEXPIREAT, HPEXPIREAT, HTTL, HPTTL, HEXPIRETIME, HPEXPIRETIME, HPERSIST | HASH section here (condition table over absolute stamps, past-time deletes, key death, the full error grammar) |
| SADD, SREM, SISMEMBER, SMISMEMBER, SCARD, SMOVE | SET section here |
| SMEMBERS, SSCAN | SET section here (listpack-tier insertion order and ascending intsets, where both sides agree; see the order note below) |
| SPOP, SRANDMEMBER | deterministic rows here (misses, count 0, one-member forcing, overpop); distribution lives in the spop lab |
| SINTER, SUNION, SDIFF, SINTERCARD | integer replies, single-member results, and absent-key rows here; wider results pinned through the STORE variants and SMISMEMBER probes (order note below) |
| SINTERSTORE, SUNIONSTORE, SDIFFSTORE | SET section here (integer replies, empty-result key delete, TTL drop, type overwrite) |
| ZADD (all flags), ZINCRBY, ZSCORE, ZMSCORE, ZREM, ZCARD | ZSET section here (score printing incl. infinities and -0, the flag conflict table, the NaN doors) |
| ZRANK, ZREVRANK | ZSET section here (WITHSCORE forms included) |
| ZRANGE (all BY forms, REV, LIMIT), ZRANGEBYSCORE, ZREVRANGEBYSCORE, ZRANGEBYLEX, ZREVRANGEBYLEX, ZREVRANGE, ZRANGESTORE | ZSET section here (zsets are ordered on both sides, so multi-member replies pin byte for byte at every tier; see the order note below) |
| ZCOUNT, ZLEXCOUNT | ZSET section here |
| ZPOPMIN, ZPOPMAX, ZMPOP, BZPOPMIN, BZPOPMAX, BZMPOP | ZSET section here (blocking forms on immediate service only; the blocked path is pinned live in the server zpop tests) |
| ZREMRANGEBYRANK, ZREMRANGEBYSCORE, ZREMRANGEBYLEX | ZSET section here (whole-window key death included) |
| ZRANDMEMBER | deterministic rows here (misses, count 0, one-member forcing); distribution is rank-descent sampling pinned in the zset tests |
| ZSCAN | ZSET section here (any-cursor small tier; the listpack-tier multi-member row pins because both sides walk it score-sorted) |
| ZUNION, ZINTER, ZDIFF (+STORE), ZINTERCARD | ZSET section here (WITHSCORES pins the WEIGHTS and AGGREGATE arithmetic, sets join at score 1, empty-result key delete, TTL drop, type overwrite) |
| GEOADD, GEOPOS, GEODIST, GEOHASH | GEO section here (flag table, all-validate-before-write, every unit, the 11-char hashes) |
| GEOSEARCH, GEOSEARCHSTORE, GEORADIUS, GEORADIUS_RO, GEORADIUSBYMEMBER, GEORADIUSBYMEMBER_RO | GEO section here (shapes, decorations, COUNT and ANY, the antimeridian wrap, STORE arms, the _RO walls, the full door table; see the geo notes below) |
| LPUSH, RPUSH, LPUSHX, RPUSHX, LPOP, RPOP | LIST section here (multi-element pushes, counted pops, both miss shapes, overpop key death) |
| LLEN, LINDEX, LSET, LRANGE | LIST section here (lists are ordered on both sides, so multi-element replies pin byte for byte at every tier; see the encoding note below) |
| LTRIM | LIST section here (interior window, identity, empty-window key death) |
| LINSERT, LREM, LPOS | LIST section here (pivot sides, the case-insensitive token, the LREM count grammar, the full LPOS RANK/COUNT/MAXLEN grammar and its option errors) |
| LMOVE, RPOPLPUSH, LMPOP | LIST section here (all four end pairs, same-key rotation, same-end identity, first-non-empty-key wins, the numkeys and direction doors) |
| BLPOP, BRPOP, BLMOVE, BRPOPLPUSH, BLMPOP | LIST section here (immediate service only plus the timeout doors that answer before blocking; the blocked path is pinned live in the server list tests) |
| XADD, XLEN | STREAM section here (explicit IDs only, bare ms as ms-0, ms-* fill, the ordering refusals, NOMKSTREAM, the field pairing door; auto IDs carry the wall clock and are pinned live in the stream tests) |
| XRANGE, XREVRANGE | STREAM section here (explicit and bare-ms bounds, exclusive bounds, COUNT, the empty window, the bound grammar doors) |
| XTRIM | STREAM section here (exact MAXLEN and MINID through XTRIM and the XADD arms, trim to empty keeps the key, the LIMIT-needs-~ door; ~ trim counts cut at engine-defined node boundaries and stay in the stream trim tests) |
| XSETID | STREAM section here (forward and equal moves, the smaller-than-top and missing-key refusals, the option arms, generation resuming above) |
| XDEL | STREAM section here (key check before ID parse, one bad ID aborts, duplicates count once, delete to empty keeps the key and the last generated ID) |
| XGROUP CREATE, SETID, CREATECONSUMER, DELCONSUMER, DESTROY | STREAM section here (BUSYGROUP, MKSTREAM, the NOGROUP walls, destroy sweeping the pending state) |
| XREADGROUP, XACK | STREAM section here (> against history, COUNT, NOACK, ack idempotence, the meaningless-$ door; the blocked path is pinned live in the server stream tests) |
| XPENDING | STREAM section here (summary form only; the extended form renders wall-clock idle ms and is pinned live in the pending tests) |
| XCLAIM, XAUTOCLAIM | STREAM section here (entry and JUSTID forms at idle 0, silent skip of unknown IDs, FORCE minting, the cursor walk and the deleted-entry drain, the numeric doors) |
| XREAD | STREAM section here (non-blocking reads, multi-stream with a miss, the frozen $, the + newest-entry form, immediate service under BLOCK, the grammar doors) |
| XINFO | GROUPS rows here (counts, IDs, and lag only); XINFO STREAM renders the radix-tree geometry fields, and CONSUMERS and FULL carry idle and delivery times, so all three are pinned live in the stream wire tests |

TYPE, TTL, OBJECT ENCODING, and DEL rows appear as scaffolding where a
string behavior needs them (TTL discard and KEEPTTL rules, encoding
boundaries); their own families are keyspace milestones.

## Divergences found and fixed while pinning

- SSCAN with a NOVALUES token answers a dedicated error in Redis 8.8
  ("NOVALUES option can only be used in HSCAN"), because the scan
  option grammar is shared across the scan family and the option is
  gated after parsing; sqlo1 fell through to the generic syntax
  error. Fixed in sscanCmd.

- GETRANGE with a still-negative end after adding the length clamps to
  byte 0 in Redis (GETRANGE v -100 -50 is the first byte), and the
  same rule holds for BITCOUNT and BITPOS ranges; sqlo1's shared clamp
  returned an empty window. Fixed in clampRange.
- OBJECT ENCODING on a missing key is a null bulk in Redis 8.8, not
  the "no such key" error older releases sent. Fixed in server.go.
- The HEXPIRE set family's grammar errors moved between releases: 8.8
  answers "unknown argument: X" for a bad condition token or a
  misplaced FIELDS after a valid condition, and a plain "wrong number
  of arguments" when numfields overshoots the args, while the read
  family (HTTL and friends) kept the older Mandatory-FIELDS and
  numfields-match texts. sqlo1 had implemented the t_hash.c 8.0 texts
  for both families. Fixed in hexpireCmd/hfeFields.
- An inline hash holding a field TTL answers listpackex to OBJECT
  ENCODING, not listpack. Fixed in Hash.Encoding.
- LPOS's RANK-zero error text shortened between releases: 8.8 answers
  "RANK can't be zero: use 1 to start from the first match, 2 from
  the second ... or use negative to start from the end of the list";
  sqlo1 had implemented the older t_list.c sentence. Fixed in
  lposCmd.

## Documented divergences that stand

- OBJECT ENCODING reports rope for values over the rope boundary,
  where Redis says raw. The representation genuinely differs and
  hiding it would cost an existence check on every encoding probe.
- The hash encoding count boundary differs: sqlo1 goes segmented
  (hashtable) past 128 fields or 2 KiB inline bytes, Redis 8.8's
  defaults are 512 entries and 64 B values. The fixture crosses the
  boundary through the value-size wall, which both sides share in
  spirit, and does not assert encoding in the 129-511 field window.
- Redis keeps listpackex once a field TTL has existed, even after the
  last TTL is persisted; sqlo1 reports listpack again the moment no
  field carries one, since the inline region genuinely holds no TTL
  bytes then.
- Set replies are unordered by contract and the orders genuinely
  differ where the representations do: Redis walks a numerically
  sorted intset or a hashtable, sqlo1 keeps insertion order inline
  and emits in fh order once segmented, algebra included. The
  fixture only pins multi-member arrays where the orders provably
  agree (listpack insertion order, intsets inserted ascending) and
  covers everything wider through SCARD, SINTERCARD, the STORE
  variants' integer replies, and SMISMEMBER probes.
- The set encoding count boundaries differ the same way the hash's
  do: sqlo1 segments (hashtable) past 128 members or 2 KiB inline
  bytes, Redis 8.8's defaults are 512 for both set-max-intset-entries
  and set-max-listpack-entries. The fixture crosses through the
  member-size wall (members over 64 B leave Redis's listpack, and 30
  of them blow sqlo1's byte cap) and does not assert encoding in the
  129-511 member window.
- Zset order is a contract, not an accident: both sides sort by
  (score, member) at every tier, so unlike sets the ZSET section pins
  multi-member replies byte for byte across the encoding boundary,
  and the boundary itself agrees for once (both sides leave listpack
  past 128 members; only the member-size wall crosses at different
  counts, 65 B for redis's per-member cap against 30 wide members for
  sqlo1's 2 KiB inline byte cap, so encoding is not asserted between
  those walls). The listpack-tier ZSCAN row pins for the same reason:
  redis walks its score-sorted listpack and sqlo1 its sorted inline
  root. Segmented-tier ZSCAN order is engine-defined (fh order) and
  stays in the zscan tests.
- The list encoding boundary differs in kind, not just figure: redis
  8.8's default list-max-listpack-size is -2, a pure byte cap of
  8 KiB with no entry-count wall, so 300 small elements are still
  listpack there, while sqlo1 goes noded (quicklist) past 128
  entries or 2 KiB inline. Lists are ordered, so every reply still
  pins byte for byte across the divergent window; the fixture only
  asserts encoding at 128 small entries (listpack on both) and past
  a 12 KiB push (quicklist on both), and sqlo1's ladder is one way
  where redis downgrades a shrunk quicklist back to listpack.
- Geo search rows always carry a sort or land in a STORE dest: the
  unsorted, uncounted emission order is the cell walk's, which is
  engine-defined on both sides (redis walks its nine neighbor cells,
  sqlo1 its cover intervals). Dest zsets are score-ordered, so the
  no-sort STORE rows still pin byte for byte.
- GEOSEARCHSTORE and GEORADIUS STOREDIST dests are pinned through
  ZCARD and memberwise ZRANGE, never ZSCORE: full-precision distances
  carry the last ulp of the libm (sin, cos, and asin are not
  correctly rounded, and Go's kernels differ from any given
  platform's there; redis builds differ across platforms the same
  way). The values agree to 14 significant digits, and every printed
  grain (WITHDIST and GEODIST at four decimals, GEOPOS at shortest
  round-trip) is pinned byte for byte in the GEO section. Plain STORE
  dests hold the 52-bit geohash bits, exact integers, and read back
  fully.
- XINFO STREAM's radix-tree-keys and radix-tree-nodes report sqlo1's
  own run and page geometry, since the listpack radix tree does not
  exist here; every other header field (length, IDs, entries-added,
  recorded-first-entry-id) matches and is pinned in the wire tests.
  XTRIM's ~ variant trims at whole-node boundaries on both sides, but
  the node geometry is engine-defined (redis's listpack fill against
  sqlo1's run cut), so approximate trim counts genuinely differ and
  only the exact forms pin here.
- INCRBYFLOAT arithmetic runs at float64. Redis computes in long
  double, and redis builds disagree with each other here: on macOS
  long double is 64-bit and 10.5 + 0.1 prints 10.59999999999999964,
  while the Linux gate rivals compute in 80-bit x87 and print 10.6,
  which is also what sqlo1 prints (verified against the frozen 8.8.0
  on the gate box). Values needing more than 53 mantissa bits stay
  different: Linux Redis answers 4.00000000000000001 to
  3 + 1.000000000000000005 where sqlo1 answers 4. Go has no long
  double; the fixture keeps to exactly-representable increments and
  the suite treats the tail digits as out of scope.
